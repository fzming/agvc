import { createNamespacedHelpers } from "vuex";
export const {
  mapMutations: mapOrgMutations,
  mapGetters: mapOrgGetters,
  mapActions: mapOrgActions,
  mapState: mapOrgState,
} = createNamespacedHelpers("orgnization");
